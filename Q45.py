#coding:utf-8

from numpy import *

Matrix = [[0, 0, 0],
          [0, 0, 0],
          [0, 0, 0]]

data1 = mat(zeros((3, 3)))
data2 = mat(Matrix)

def process(M):

    # for i in range(len(M)):
    #     for j in range(len(M[0])):
    #         M[i][j] -= 1
    #         if i - 1 > 0 or i + 1 < len(M):
    #             M[i-1][j] -= 1
    #             M[i+1][j] -= 1
    #
    #         if j - 1 > 0 or j < 1 < len(M[0]):
    #             M[]
    pass






process(Matrix)









